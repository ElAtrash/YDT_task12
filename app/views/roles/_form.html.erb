<%= form_with(model: role, local: true) do |form| %>
  <% if role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
        <% role.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :create %>
    <%= form.check_box :create %>
  </div>

  <div class="field">
    <%= form.label :read %>
    <%= form.check_box :read %>
  </div>

  <div class="field">
    <%= form.label :update %>
    <%= form.check_box :update %>
  </div>

  <div class="field">
    <%= form.label :delete %>
    <%= form.check_box :delete %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
